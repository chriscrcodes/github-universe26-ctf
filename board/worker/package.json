{
  "name": "@github-universe26/board-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply BOARD_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply BOARD_DB --remote"
  },
  "dependencies": {
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "wrangler": "^4.134.0"
  }
}
